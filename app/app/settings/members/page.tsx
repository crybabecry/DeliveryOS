"use client";
import { useState } from "react";
export default function MembersPage(){const [email,setEmail]=useState("");const [role,setRole]=useState("VIEWER");const [status,setStatus]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setStatus(null);const r=await fetch("/api/members/invite",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,role})});const d=await r.json();setStatus(r.ok?"Invitation sent.":d.error||"Invitation failed.");}return <div className="content narrow"><div style={{marginBottom:22}}><h1 className="page-title">Members</h1><p className="page-subtitle">Invite colleagues into the current organisation.</p></div><section className="card"><form className="form" onSubmit={submit}><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Role<select className="select" value={role} onChange={e=>setRole(e.target.value)}><option>VIEWER</option><option>ENGINEER</option><option>QA</option><option>MANAGER</option><option>ADMIN</option></select></label><button className="btn btn-primary">Send invitation</button></form>{status?<div className="notice notice-success">{status}</div>:null}<p className="metric-note" style={{marginTop:12}}>Requires the server-only Supabase service role key for invitation emails.</p></section></div>}
